name: "Tag on Auto-Update Merge"

on:
  pull_request:
    types: [closed]

jobs:
  tag:
    # only when an auto-update PR is actually MERGED (not merely closed)
    if: >
      github.event.pull_request.merged == true &&
      startsWith(github.event.pull_request.head.ref, 'auto-update/')
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Merged Commit
      uses: actions/checkout@v6
      with:
        ref: ${{ github.event.pull_request.merge_commit_sha }}
        token: ${{ secrets.RELEASE_PAT }}  # PAT so the tag push triggers build_and_publish_release.yml
        fetch-depth: 0                      # need existing tags for the idempotency check

    - name: Extract Iosevkata Version
      id: version
      run: echo "version=$(./scripts/get-iosevkata-version.sh)" >> "$GITHUB_OUTPUT"

    - name: Extract Dependency Versions
      id: deps
      run: ./scripts/get-dependency-versions.sh >> "$GITHUB_OUTPUT"

    - name: Create and Push Tag
      env:
        VERSION: ${{ steps.version.outputs.version }}
        IOSEVKA_VERSION: ${{ steps.deps.outputs.iosevka }}
        NERDFONTS_VERSION: ${{ steps.deps.outputs.nerdfonts }}
      run: |
        tag="v$VERSION"
        git fetch --tags --quiet
        if git rev-parse "$tag" >/dev/null 2>&1; then
          echo "Tag $tag already exists, skipping."
          exit 0
        fi
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        # The tag annotation is what GitHub renders as the release notes,
        # since build_and_publish_release.yml creates the release without a body.
        printf 'Iosevka v%s\nnerd-fonts v%s\n' "$IOSEVKA_VERSION" "$NERDFONTS_VERSION" \
          | git tag -a "$tag" -F -
        git push origin "$tag"
        echo "Created and pushed $tag."
